{
  "name": "part3",
  "version": "1.0.0",
  "description": "backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "unix_build:ui": "rm -rf build && cd ../part2-notes/ && npm run build && cp -r build ../notes-backend",
    "build:ui": "@powershell Remove-Item -Recurse -Force build && cd ../part2 && npm run build && @powershell Copy-Item build -Recurse ./",
    "deploy": "gcloud app deploy",
    "deploy:full": "npm run build:ui && npm run deploy",    
    "app:open": "gcloud app browse"
  },
  "author": "Vitkai",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
